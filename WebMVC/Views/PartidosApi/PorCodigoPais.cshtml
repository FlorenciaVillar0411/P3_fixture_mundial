@model WebMVC.Models.BusquedaPartidoViewModel

@{ ViewData["Title"] = "PorCodigoPais"; }


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Pr Codigo Pais</title>
</head>
<body>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <div class="row">
        <div class="col-md-4">
            <form asp-action="PorCodigoPais">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label name="CodigoPais" asp-for="CodigoPais">Codigo Pais:</label>
                    <input type="text" id="Paises" name="CodigoPais" asp-for="CodigoPais">
                </div>

                <div class="form-group">
                    <input type="submit" value="Buscar" class="btn btn-primary" />
                </div>
            </form>
        </div>
    </div>
    <div>
        @ViewBag.Error
    </div>

    <table class="table">
        <thead>
            <tr>
                <th>
                    Id partido
                </th>
                <th>
                    Id seleccion 1
                </th>
                <th>
                    Id seleccion 2
                </th>
                <th>
                    Fecha
                </th>
                <th>
                    Hora
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Partidos)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdEquipoUno)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.IdEquipoDos)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Fecha)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Hora)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>

